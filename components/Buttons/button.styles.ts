import { StyleSheet } from "react-native";

export default StyleSheet.create({
  defaultContainer: {
    backgroundColor: "#6600FF", // purple
    padding: 10,
    borderRadius: 50,
    flexDirection: "row",
  },
  defaultText: {
    color: "#fff", // white
    textAlign: "center",
    fontWeight: "bold",
  },
  image: {
    width: 20,
    height: 20,
    marginRight: 10,
  },
});
